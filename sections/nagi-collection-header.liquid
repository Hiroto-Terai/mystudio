<section class="nagi-collection-header">
  <div class="nagi-collection-header__inner">
    {%- if section.settings.eyebrow != blank -%}
      <span class="nagi-section-eyebrow">{{ section.settings.eyebrow }}</span>
    {%- endif -%}
    <h1 class="nagi-collection-header__heading">{{ collection.title }}</h1>
    {%- if section.settings.show_description and collection.description != blank -%}
      <div class="nagi-collection-header__description">{{ collection.description }}</div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Nagi Collection Header",
  "tag": "div",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "眉書き (英語)",
      "default": "Collection"
    },
    {
      "type": "checkbox",
      "id": "show_description",
      "label": "コレクションの説明文を表示",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Nagi Collection Header"
    }
  ]
}
{% endschema %}
