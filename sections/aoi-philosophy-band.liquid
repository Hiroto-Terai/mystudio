<section class="aoi-philosophy">
  <div class="aoi-philosophy__inner">
    <div class="aoi-philosophy__image-wrap">
      {%- if section.settings.image != blank -%}
        {{ section.settings.image | image_url: width: 900 | image_tag: loading: 'lazy' }}
      {%- else -%}
        {{ 'lifestyle-2' | placeholder_svg_tag: 'placeholder-svg' }}
      {%- endif -%}
    </div>

    <div>
      {%- if section.settings.eyebrow != blank -%}
        <span class="aoi-section-eyebrow" style="color:var(--aoi-green-70)">{{ section.settings.eyebrow }}</span>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="aoi-section-heading" style="margin:12px 0 16px">{{ section.settings.heading | replace: '<p>', '' | replace: '</p>', '' }}</h2>
      {%- endif -%}
      {%- if section.settings.body != blank -%}
        <p class="aoi-philosophy__body">{{ section.settings.body }}</p>
      {%- endif -%}
      {%- if section.settings.cta_label != blank -%}
        <a href="{{ section.settings.cta_url }}" class="aoi-btn aoi-btn--primary" style="height:44px">
          {{ section.settings.cta_label }}
        </a>
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "aoi Philosophy Band",
  "tag": "div",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "画像"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "眉書き",
      "default": "Our Philosophy"
    },
    {
      "type": "richtext",
      "id": "heading",
      "label": "見出し",
      "default": "<p>肌本来の力を、<br>植物が引き出す。</p>"
    },
    {
      "type": "textarea",
      "id": "body",
      "label": "本文",
      "default": "私たちは、必要なものだけを選び抜いた処方にこだわります。国産のボタニカル成分と、余計なものを加えないシンプルなレシピで、敏感な肌にもやさしく寄り添います。"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "ボタン テキスト",
      "default": "ブランドストーリー"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "ボタン URL"
    }
  ],
  "presets": [
    {
      "name": "aoi Philosophy Band"
    }
  ]
}
{% endschema %}
